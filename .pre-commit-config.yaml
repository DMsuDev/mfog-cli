# =============================================================================
# Pre-commit configuration for Node.js / pnpm projects
# https://pre-commit.com
#
# Install:  pre-commit install   # runs on every git commit
# Run all:  pre-commit run --all-files
# Update:   pre-commit autoupdate
# =============================================================================

default_language_version:
  python: python3

ci:
  autofix_commit_msg: "style: auto fixes from pre-commit"
  autoupdate_commit_msg: "chore: autoupdate pre-commit hooks"
  autoupdate_schedule: weekly
  submodules: false

repos:
  # === General file hygiene ==============================================
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace # Remove trailing whitespace
        args: [--markdown-linebreak-ext=md]
      - id: end-of-file-fixer # Ensure files end with newline
      - id: check-yaml # Validate YAML syntax
      - id: check-json # Validate JSON syntax
      - id: check-xml # Validate XML syntax
      - id: check-merge-conflict # Detect merge conflict markers
      - id: check-added-large-files # Prevent giant files from being committed
        args: [--maxkb=1024]
        exclude: ^(node_modules|dist|build|coverage|templates)/
      - id: mixed-line-ending # Detect mixed line endings
        args: [--fix=auto]
      - id: check-case-conflict # Check for files that would conflict on case-insensitive FS
      - id: fix-byte-order-marker # Remove UTF-8 BOM

  # === Markdown lint ====================================================
  - repo: https://github.com/igorshubovych/markdownlint-cli
    rev: v0.48.0
    hooks:
      - id: markdownlint
        name: Lint markdown files (with --fix)
        args: [--fix]
        types: [markdown]
        exclude: ^node_modules/

  # === Spell checking ==================================================
  - repo: https://github.com/codespell-project/codespell
    rev: v2.4.2
    hooks:
      - id: codespell
        name: Lint spelling in source files (with --fix)
        args:
          - "--ignore-words-list=nd,ot,te"
          - "--skip=pnpm-lock.yaml,package-lock.json,node_modules,dist,build,coverage"
        exclude: ^(node_modules|dist|build|coverage)/

  # === YAML ============================================================
  - repo: https://github.com/google/yamlfmt
    rev: v0.21.0
    hooks:
      - id: yamlfmt
        name: Format YAML files with yamlfmt
        args:
          - --formatter
          - "indent=2,include_document_start=false,retain_line_breaks=true"
        exclude: ^(\.pre-commit-config\.yaml|\.markdownlint\.yaml|pnpm-lock\.yaml|node_modules|dist|build|coverage)(/|$)

  # === Prettier =========================================================
  # pre-commit/mirrors-prettier is archived; rbubley/mirrors-prettier tracks stable releases
  - repo: https://github.com/rbubley/mirrors-prettier
    rev: v3.8.3
    hooks:
      - id: prettier
        name: Format frontend files with Prettier
        types_or:
          - javascript
          - jsx
          - ts
          - tsx
          - json
          - yaml
          - markdown
          - css
          - scss
          - html
        exclude: ^(\.pre-commit-config\.yaml|\.markdownlint\.yaml|pnpm-lock\.yaml|package-lock\.json|node_modules|dist|build|coverage)(/|$)

  # === ESLint (uses project flat config via pnpm) =======================
  - repo: local
    hooks:
      - id: eslint
        name: Lint JavaScript / TypeScript
        entry: pnpm exec eslint --fix
        language: system
        files: \.(js|mjs|cjs|jsx|ts|tsx)$
        pass_filenames: true
        require_serial: true
        exclude: ^(node_modules|dist|build|coverage)/
